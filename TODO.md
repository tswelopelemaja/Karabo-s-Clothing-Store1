# TODO

- [ ] Replace broken `products.html` product markup with a clean `<div class="product-grid">` wrapper and `<div class="product-card">` for each product.
- [ ] Add CSS rules for `.product-grid` and `.product-card` in `style.css`.
- [ ] Keep the rest of the page layout consistent with existing `header/nav/section` styling.
- [ ] Validate the HTML structure (no invalid nesting) and ensure CSS is applied.

